name: Sonda Mapa de la Deuda

on:
  pull_request:
    branches: [main]
    paths:
      - 'sonda_mapadeuda.py'
      - '.github/workflows/sonda-mapadeuda.yml'
  workflow_dispatch:

jobs:
  sonda:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests
      - run: python sonda_mapadeuda.py
