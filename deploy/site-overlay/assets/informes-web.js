document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('[data-copy-citation]').forEach(function(button){button.addEventListener('click',async function(){var text=document.querySelector('[data-citation-text]');if(!text)return;try{await navigator.clipboard.writeText(text.textContent.trim());button.textContent='Cita copiada ✓';button.classList.add('is-copied');setTimeout(function(){button.textContent='Copiar cita';button.classList.remove('is-copied')},2200)}catch(error){button.textContent='Seleccioná y copiá la cita'}})})});
