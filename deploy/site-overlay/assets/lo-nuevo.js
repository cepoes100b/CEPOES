(()=>{'use strict';const cards=[...document.querySelectorAll('.new-card')],search=document.getElementById('new-search'),type=document.getElementById('new-type'),topic=document.getElementById('new-topic'),total=document.getElementById('new-total'),empty=document.getElementById('new-empty');const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();function render(){const q=norm(search.value),t=type.value,p=topic.value;let shown=0;cards.forEach(card=>{const ok=(!q||norm(card.textContent).includes(q))&&(!t||card.dataset.type===t)&&(!p||card.dataset.topic===p);card.hidden=!ok;if(ok)shown++});total.textContent=`${shown} ${shown===1?'novedad':'novedades'}`;empty.hidden=shown!==0}search.addEventListener('input',render);type.addEventListener('change',render);topic.addEventListener('change',render);render()})();
